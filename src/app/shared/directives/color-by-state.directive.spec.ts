import { ColorByStateDirective } from './color-by-state.directive';

describe('ColorByStateDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorByStateDirective();
    expect(directive).toBeTruthy();
  });
});
